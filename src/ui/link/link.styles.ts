import styled from 'styled-components';

export default styled.a`
	color: #8fa0ff;
	font-weight: 800;
	font-size: inherit;
	text-decoration: none;
	outline: none;
	cursor: pointer;

	&:link {
		color: #8fa0ff;
	}

	&:visited {
		color: #8fa0ff;
	}

	&:hover {
		color: #8fa0ff;
	}

	&:active {
		color: #8fa0ff;
	}
`;
