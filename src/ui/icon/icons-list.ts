import * as icons from '@icons';

const allIconsList = {
	avatar: icons.AvatarIcon,
	cookie: icons.CookieIcon,
	exit: icons.ExitIcon,
	'eye-off': icons.EyeOffIcon,
	'eye-on': icons.EyeOnIcon,
	person: icons.PersonIcon
};

export default allIconsList;
